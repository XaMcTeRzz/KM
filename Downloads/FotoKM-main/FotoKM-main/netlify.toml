[build]
  command = "npm install && npm run build:netlify"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22.11.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
